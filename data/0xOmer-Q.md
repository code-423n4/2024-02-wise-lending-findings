In addition to `_increaseFeeTokens()` which executes for one, consider adding a 
bulk function to take an array of `_FeeTokens` and `_amount`. It also applies for `_decreaseFeeTokens()` and `setAaveFlag()`.